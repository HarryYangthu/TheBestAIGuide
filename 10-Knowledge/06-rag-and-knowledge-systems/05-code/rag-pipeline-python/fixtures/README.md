# 教学语料与查询标注

> 状态：verified
> 数据版本：synthetic-fixtures-v1；2026-09-06；人工构造，非真实设备文档。

[corpus.jsonl](corpus.jsonl)有 8 条文档写入事件。alpha/fan-guide 的 v1 被 v2 替换；beta 同名文档属于不同租户，不得互相覆盖。ALM-12003、ALM-12030 用于相似编码反例；DEMO-A/DEMO-B 用于产品过滤；字段数据未从任何厂商手册复制。

[queries.jsonl](queries.jsonl)有 8 条查询：2 条编号，2 条词法，1 条跨语言，3 条应返回空的版本/权限/无答案查询。`relevance` 的 2 表示直接相关，省略项视为 0。它是固定教学 gold，来源依据为本文件作者定义的 fixture 关系；未进行独立专家标注和模型 Judge 校准。

查询 ID 是评分记录字段，不传给检索器。此小集合全部可见，用于理解与回归，不拆出伪装成独立的隐藏测试集。实验只比较明确实现的通道；真实语义模型需在增加自然表达与独立标注后重新评估。
