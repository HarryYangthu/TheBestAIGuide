[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
[project]
name = "aiguide-tiny-transformer"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["torch==2.8.0"]
[tool.setuptools.packages.find]
where = ["src"]
