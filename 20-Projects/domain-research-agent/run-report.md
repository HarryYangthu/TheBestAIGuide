# 综合项目运行记录

> 运行日期：2026-09-06；Python 3.12.13；Linux；本地构造语料，无网络模型调用。

执行命令见 [README](README.md)。六个集成测试通过：引用与记忆、无证据与跨租户拒答、模型不能改权限或直接发布无证据结论、两个崩溃位置恢复、请求版本变化拒绝复用、模型不能把错误码改成另一编号。最后一项来自审查发现的实际反例：原文真实不代表回答了原问题，因此检索输入也要绑定任务约束。

评测使用四个任务，每个执行两次，各 Trial 新建临时目录。八次均通过预先写好的回答及状态断言，系统错误为零。重复的是确定性程序，不是八个独立抽样问题，不据此计算真实成功率或置信区间。

| 任务 | 检查内容 | 本次通过 |
| --- | --- | --- |
| supported | 一条可定位引用，报告和引用记忆已写入 | 2 / 2 |
| unknown | 无对应编号时拒答，无引用、无事实记忆 | 2 / 2 |
| private | 其他租户的构造记录不进入输出 | 2 / 2 |
| resume | 报告写出后故障，重开服务仍可完成 | 2 / 2 |

机器证据：[summary.json](evaluation/report/summary.json)、[trials.jsonl](evaluation/report/trials.jsonl)。延迟是本机耗时，不是模型或生产服务性能。未验证真实 LLM、开放网页、并发多写者、主机断电或分布式副作用。
